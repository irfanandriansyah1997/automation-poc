import { test } from '@playwright/test';

test.describe(
  'Testing credit card form page should be working properly',
  { tag: ['@payment-gateway', '@login-page'] },
  () => {
    test('Should be render correctly', async () => {});
  }
);
